import { Inter, Alike, Tangerine } from "next/font/google";

export const inter = Inter({
  subsets: ["latin"],
  display: "swap",
});

export const alike = Alike({
  subsets: ["latin"],
  weight: "400",
});
